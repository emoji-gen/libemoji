box: debian
build:
  steps:
    - script:
        name: echo
        code: |
          apt-get update -qq
          apt-get install git cmake g++ python libfontconfig1-dev libx11-dev libxcomposite-dev libgl1-mesa-dev libglu1-mesa-dev freeglut3-dev -y

          git submodule update --init

          cmake .
          make
